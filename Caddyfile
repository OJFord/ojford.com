ojford.com {
    tls {
        dns cloudflare
    }

    gzip

    log /var/log/access.log {
        rotate {
            size 100
        }
    }

    proxy / https://ojford.keybase.pub/envelope/ {
        except .well-known/* assets/*
    }
    
    root static
}
